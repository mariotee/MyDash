export default (theme) => ({
  tableTitle: {
    margin: `${theme.spacing.unit * 3}px`,
    display: 'flex',
    justifyContent: 'space-between',
  },
  searchbar: { width: `${theme.spacing.unit * 20}px` },
});
